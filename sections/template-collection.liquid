<div class="px-4 py-8">
    
    {% paginate collection.products by section.settings.number_of_collection %}
        <h1 class="text-3xl text-center">{{ collection.title }}</h1>
    
        <div class="max-w-6xl mx-auto">
            <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                {% for product in collection.products %}
                        <!-- Render's a product card -->
                        <!-- Pass the product value from the loop -->
                    {% render 'product-card', product: product %}
                
                {% else %}
                        <p>There are no products inside of this collection</p>
                {% endfor %}
            </div>
        </div>
      
    
        {% render 'pagination', pagination: paginate %}
    {% endpaginate %}
    
    </div>
    
    {% schema %}
    {
        "name": "Collection",
        "settings": [
            {
                "type": "range",
                "id": "number_of_collection",
                "label": "Collection Per page",
                "step": 1,
                "min": 1,
                "max": 6,
                "default": 5
            }
        ]
    }
    {% endschema %}